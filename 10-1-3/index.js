import React from 'react';
import ReactDOM from 'react-dom/client';
import PageColor from './chapter12/PageColor';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
    <PageColor />
);
